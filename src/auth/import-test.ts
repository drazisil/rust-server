import { checkCredentials } from './checkCredentials';

// This file is only for TypeScript module resolution testing.
// It will be imported by a test runner to verify import works.

console.log('checkCredentials import works:', typeof checkCredentials === 'function');
